apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Apply with: kubectl apply -k infrastructure/kubernetes/cert-manager/
# This is separate from base/staging/production — applied once at cluster setup time.
resources:
  - cluster-issuer.yaml
