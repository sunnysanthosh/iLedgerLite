[tool.ruff]
target-version = "py312"
line-length = 120

[tool.ruff.lint]
select = ["E", "F", "I", "W"]
ignore = ["E501", "F821"]  # F821: false positives for SQLAlchemy relationship() string forward refs

[tool.coverage.report]
fail_under = 60
omit = ["tests/*", "conftest.py"]
